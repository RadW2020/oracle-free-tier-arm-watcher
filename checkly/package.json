{
  "name": "uliber-monitoring",
  "version": "1.0.0",
  "private": true,
  "description": "Configuration as code for the Checkly account (Uliber & Co)",
  "scripts": {
    "validate": "checkly validate",
    "test": "checkly test",
    "deploy": "checkly deploy",
    "deploy:preview": "checkly deploy --preview",
    "import:plan": "checkly import plan",
    "import:apply": "checkly import apply",
    "import:commit": "checkly import commit",
    "import:cancel": "checkly import cancel"
  },
  "devDependencies": {
    "@checkly/playwright-helpers": "^1.0.4",
    "@playwright/test": "^1.50.0",
    "@types/node": "^26.2.0",
    "checkly": "^6.9.8",
    "ts-node": "^10.9.2",
    "typescript": "5.9.2"
  }
}
