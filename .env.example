# Server configuration
PORT=8088

# API Key for endpoint authentication (optional but recommended)
# Generate a secure random string, e.g.: openssl rand -hex 32
# If not set, endpoints will be publicly accessible
API_KEY=your-secret-api-key-here

# Oracle Cloud Infrastructure credentials
# Get these from: OCI Console -> Profile -> API Keys
OCI_TENANCY_ID=ocid1.tenancy.oc1..xxxxx
OCI_USER_ID=ocid1.user.oc1..xxxxx
OCI_FINGERPRINT=xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx
OCI_PRIVATE_KEY_PATH=/path/to/your/oci_api_key.pem
OCI_REGION=eu-madrid-1

# Compartment to monitor (usually same as tenancy for free tier)
OCI_COMPARTMENT_ID=ocid1.compartment.oc1..xxxxx

